[project]
name = "ansible"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ansible-core>=2.15",
    "netmiko>=4.0",
    "paramiko>=3.0",
]
